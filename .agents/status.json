{
  "swarm": "speaker-improvement",
  "mode": "supervised-burst",
  "version": 1,
  "machine": {
    "cpu": "i9-13980HX (32 threads)",
    "ram_gib": 30,
    "gpu": "RTX 4090 Laptop (16 GB)",
    "python": "3.12",
    "target_profile": "desktop_gpu_4090"
  },
  "last_verdict": {
    "tests": "1754 passed, 10 skipped (.venv/bin/python -m pytest tests -m 'not real_model and not recorded and not live_output')",
    "when": "2026-06-14",
    "machine": "i9-13980HX"
  },
  "next": "DONE 2026-06-14 pt7 (COMPUTER-USE -- SAFETY FOUNDATION + READ-ONLY; merged to main; 1754 green excl. real-model tier). Started the 'take actions' capability (type/click/press/identify) the SAFE way: landed the security foundation + read-only tier; the ACTUATOR (click/type/press) is a deliberate HARD NO-GO until owner speaker-ID enrollment + a §9.7 local pin + bound-confirm + live validation. Built via a 9-agent research+audit+design workflow (Open Interpreter[AGPL not MIT!]/Anthropic computer-use schema/UI-grounding/input libs/security) + a 7-agent adversarial security review (NO blockers; 3 confirmed fixed). See docs/session_2026-06-14_computer_use_foundation.md. LANDED: (1) always_on_agent/origin.py -- the lethal-trifecta action-trust CHOKEPOINT (keystone, stdlib): Origin{LIVE_AUDIO trusted; SCREEN/WEB/MEMORY/FILE/UNKNOWN untrusted}; is_action_allowed(origin, owner_verified) = LIVE_AUDIO AND owner_verified is True (strict -- bakes in 'mic != owner'); combine() most-untrusted-wins lineage; should_block_action fail-closed; enforce_action raises ActionBlocked; origin_for_tags maps the tag vocab. Verified fail-closed vs every adversarial input. The actuator slice wires enforce_action (with real speaker-ID owner_verified) in front of EVERY GUI primitive. (2) core/ui_grounding.py READ-ONLY screen.identify: find_targets (pure, stopword-filtered OCR-box ranking) + ocr_words (pytesseract image_to_data, optional) + render_elements (PII-redact + wrap_untrusted-fence screen labels + centers). attach_computer_use_capability registers screen.identify side_effecting=False/egress=local/planner_tool=FALSE (kept off the cloud-routable planner until the §9.7 float is enforced) only when enabled; result sensitivity=private/origin=screen. NO actuator registered (verified: zero pyautogui/click/type in shipped code; no OI import). (3) Wiring DEFAULT-OFF: config.json gui_actions block (enabled:false) + --gui-actions flag + runtime gate (computer_use_config.enabled), separate opt-in from --agent. +16 tests (test_origin.py adversarial no-audio: untrusted+mic!=owner+fail-closed+lineage -> zero actions; test_ui_grounding.py read-only/fenced/redacted/default-off). REUSE decided for the actuator: pyautogui(BSD) NOT Open Interpreter(AGPL -- keep lazy/optional/off remote/); ydotool(shell-out) Wayland; pytesseract(Apache) OCR; a11y trees (pywinauto/pyatspi/pyobjc); Anthropic action schema; Set-of-Mark(owner picks, never VLM). NEXT (actuator, OWNER-INVOLVED): (a) OWNER enroll speaker-ID (python -m core --enroll) -- the precondition; (b) Slice 2 core/gui_control.py discrete primitives (pyautogui, Anthropic schema, bounds-checked) side_effecting=True/planner_tool=False behind enforce_action(owner_verified=<speaker-ID verdict>); route through EXISTING pending_confirmations with a BOUND spoken read-back (fix blind-FIFO _confirm_next); kill-switch (PAUSE/DISARM latch checked before every event + KWS stop + FAILSAFE) + rate budget + run-bundle audit; dry_run default-ON; pin ACT/screen to a LOCAL tier + forbid gui.*/screen.identify on remote/; live-validate before flipping dry_run off; (c) then make screen.identify a planner tool once the float is wired + cross-platform reach + SECURITY.md OWASP gate + optional Prompt-Guard-2. FIRST INVARIANT to keep: a keyboard/mouse/Enter action may be derived ONLY from a turn whose ENTIRE lineage is owner-verified live audio, at a single fail-closed chokepoint below planner/router/KWS. === PRIOR: DONE 2026-06-14 pt6 (AUDIT-DRIVEN HARDENING -- memory/image/prompt-injection; 4 slices merged to main; 1736 green excl. real-model tier). Audited the app vs OSS state-of-the-art (Mem0/Letta/Zep/Generative-Agents/Microsoft-spotlighting/OWASP-LLM01/Presidio/screenpipe) via a 6-agent research+audit workflow, then landed the best-fit features as 4 default-OFF, adversarially-reviewed slices (NO blockers; all review findings fixed). See docs/session_2026-06-14_audit_driven_hardening.md. SLICE 1 (b73928a->dd9dc11) PROMPT-INJECTION SPOTLIGHTING + OCR PII REDACTION -- NEW always_on_agent/untrusted.py (stdlib): wrap_untrusted() spotlights untrusted content in a per-process unguessable fence + never-obey directive (strips forged fences; flags injection); detect_injection() (obfuscation-resistant); redact_pii() (provider keys/Bearer/env-prefixed secrets/Luhn cards/SSN/email/phone). Wired at FOUR sites: recalled+last-session+vision blocks (capabilities), egress web results in the ReAct planner, egress steps in research_synth (RESEARCH plan path), OCR+caption redaction before a vision memory persists. Command/tool fast-path quarantine verified structural. SLICE 2 (4abbfee->35970fa) VISUAL: _dhash() perceptual near-dup gate (Pillow LANCZOS) replaces exact-SHA1 in observe() (SHA1 fallback; throttle pre-check; _last_phash unconditional) + structured caption prompt; memorize_phash_threshold (default 4). SLICE 3 (aecd733->e12abac) MEMORY QUALITY: multi-signal recall scoring in the shared recall.py -- RecallBudget recency_weight/recency_half_life_days/importance_weight (default 0=OFF, byte-identical), _apply_signals scales the native score by a convex [0,1] blend of recency-decay + kind-importance (profile>summary>message>vision); NEGATIVE cosine left unchanged (review-caught major). recency is single-backend by design. SLICE 4 (a265cc5->f243932) PROCEDURAL MEMORY: always_on_agent/procedural.py extract_rule() (high-precision, requires a teach FRAME; rejects bare always/never + statements + content requests + non-name 'call me' idioms) + render_rules(); procedural_rules() protocol verb on all 3 backends (RAM list exempt from working-window; SQLite whole-table tag scan + cosine-exclusion + TTL-exempt; PG rule:: profile key never-TTL'd + excluded from profile block); capture on the live query BEFORE escalation, detect_injection-guarded; injected TRUSTED (not spotlighted) + sensitivity-floated; procedural_enabled (default false). DEFAULTS: spotlight/redact ON when their feature is active (env hatches SPEAKER_DISABLE_SPOTLIGHT/REDACT); everything else default-OFF (recall_recency_weight/importance_weight=0, procedural_enabled=false, memorize_phash_threshold only when screen memorize on). DEFERRED (scouted, DB-coupled): hybrid lexical+semantic retrieval (RRF/migration 005 pg_trgm); supersede-on-contradiction (bi-temporal migration + sleep-time LLM consolidation; profile already overwrites); CLIP/SigLIP visual recall; optional local Prompt-Guard-2 injection classifier; minor profile+procedural double-injection on 'call me X'. NOTE: PG-tier flags want a real-pgvector validation pass before flipping on (new SQL is standard but unverified vs a live DB this session). OWNER still owes: rotate Gemini key + D1 history purge + speaker-ID enroll; measure flags via tools.bench before enabling. === PRIOR: DONE 2026-06-14 pt5 (GAP-ANALYSIS P2 -- MEMORY CONTINUITY-COMPLETION SLICE; merged feat/memory-continuity-completion -> main; 1681 green excl. load-flaky real-model replay). FOUR default-OFF/additive items, opening-turn byte-identical. (1) lm-5 persist+recall ASSISTANT FINALS on the Postgres tier: default-OFF memory.persist_assistant folds 'assistant' into MemoryManager.persist_roles; add_message persists the assistant final OFF the bus thread via _save_message_to_db(source='assistant_final'); search_memory relaxes its role filter to a PARAMETERIZED role = ANY(%s) admitting 'assistant' only when enabled (disabled => no write+no read => recall byte-identical). RAM+SQLite already store/recall assistant_output items -- this catches Postgres up; NO episodes table (reuses messages role='assistant'). Plumbed app._build_memory -> MemoryManagerAdapter -> create_memory_manager. (2) lm-2 Wire 3 one-shot 'Last session' recap: _seed_summary_head snapshots a SEPARATE _last_session_head (immune to rolling-summary writes); last_session_summary() exposes it ('' for RAM/SQLite/continuity-off); a PROCESS-START LATCH in attach_llm_capabilities injects '=== Last Session ===' on the FIRST one-shot answer turn only, ahead of recall, COMPRESSED to the recall budget, sensitivity-floated. Latch burns ONLY once the recap is built (transient turn-1 error stays retryable). Injection = a separate runtime-prepended string (NOT inside get_context_for_llm budget) so the budgeted recall block stays byte-identical; Postgres-only by construction. (3) Recall-B DECOUPLE profile injection from recall_enabled: new MemoryManager.get_profile_context() + profile_block() verb on the Memory protocol (RAM/SQLite return ''); capabilities one-shot path -> recall ON: context_for_llm (already includes profile), recall OFF: profile_block() (profile only, no double-inject); lm-3 sensitivity float now over the COMBINED (last-session+recall/profile) block + VISION_LABEL->PRIVATE rides the combined block. (4) lm-8 PRUNE 10 dead writer-config keys from config.json (verified zero live readers -- _build_memory never builds MemoryWriterConfig; from_mapping has no callers) + 'every memory config key is consumed' test (allow-list + REAL-READER check). Built via 5-agent scout + 15-agent adversarial review (NO blockers/majors; addressed: §9.7 coverage gaps [added combined-float + VISION->PRIVATE + escalation-exclusion tests], latch ordering [flip inside if head: + retry test], consumption-test strength [real-reader check], post-close background-write guard [_save_message_to_db pool-None early-return]). Tests: tests/test_memory_continuity_completion.py (capability-level, no DB) + tests/test_memory_lm5_pg.py (fake-pool, importorskip psycopg) + test_memory_app_wiring extensions. NOTE: real-PG end-to-end validation of lm-5 recommended before flipping persist_assistant on (gated behind default-OFF opt-in). REMAINING P2 (scouted, see docs/session_2026-06-14_memory_continuity_completion.md): recall-quality TIMESTAMPS (parity-safe: default-OFF show_age flag + render-time now threaded build_block->render->_render_line, coarse relative-age buckets only when on, ts unrendered by default, age tests single-backend); RETENTION/PROVENANCE (daily apply_retention timer on the writer thread [today only runs at stop()]; 'forget that' intent IntentKind.FORGET + delete verb; migration 005 tags/channel + ambient RAM-only + recall down-ranks ambient [lm-6]; record D8); lm-9/aq-4 FACTOR-POLICY-OUT refactor (large, parity-sensitive: shared memory_policy.py -- worthiness/clean/token-estimate/summary-trigger/topic-extract/profile-fact/ttl-cutoff; factoring worthiness into RAM/SQLite CHANGES what they ingest, guard with parity tests). Then P3 routing quality+cost, P4 installer/BargeInDetector/mobile, P5 remote auth. OWNER still owes: rotate Gemini key + D1 history purge + speaker-ID enroll; flip recall_enabled/cross_session_continuity/persist_assistant after tools.bench + real-PG validation. === PRIOR: DONE 2026-06-14 pt4 (GAP-ANALYSIS P2 -- lm-2 CROSS-SESSION CONTINUITY, Postgres tier; merged feat/memory-cross-session-continuity -> main @c0acd7f; 1662 green excl. load-flaky real-model replay). Completes continuity for Postgres (SQLite is already persistent). session_id regenerates each construction -> a restart lost the rolling-summary head + recent-messages warm start. Two wires behind a new DEFAULT-OFF flag cross_session_continuity (like recall_enabled -- opt-in/measure first): Wire 1 _seed_summary_head() seeds _summary_head from the newest summaries row (SELECT summary ... ORDER BY created_at DESC LIMIT 1) so the first rolling summary of a fresh process folds the prior head in; Wire 2 _load_recent_messages() falls back to most-recent user messages ACROSS sessions when the current session has no rows (current-session rows still win -- no stale mixing). Plumbed MemoryManager -> create_memory_manager -> MemoryManagerAdapter -> core/app._build_memory (config memory.cross_session_continuity). Wire 3 (one-shot 'Last session' block) DEFERRED -- needs injection-point decision (inside get_context_for_llm budget vs runtime-prepended). Default OFF = opening-turn context byte-identical (pinned by neutrality test). Tests: tests/test_memory_continuity.py (fake-pool unit tests, importorskip psycopg) + adapter forwarding; VERIFIED end-to-end vs real pgvector/pg16 in docker. NOTE real-model recorded-voice replay is load-flaky under full-suite concurrency (passes 7/7 in isolation; CI-excluded). REMAINING P2 (scouted): recall-quality (timestamps -- needs a parity-safe design since backends store different wall-clock ts; profile-injection decouple from recall_enabled, narrow value + lm-3 precondition); retention/provenance (daily apply_retention schedule, 'forget that' intent, migration 005 tags/channel + ambient RAM-only, record D8 -- recommended default given); lm-5 episodic (persist assistant finals: re-add memory_persist_assistant WITH a consumer, or episodes table; lm-3 precondition); lm-2 Wire 3; the lm-9/aq-4 factor-policy-out refactor + lm-8 writer-config wiring/consumption test. Then P3 routing quality+cost, P4 installer/BargeInDetector/mobile, P5 remote auth. OWNER still owes: rotate Gemini key + D1 history purge + speaker-ID enroll; flip recall_enabled (and now cross_session_continuity) after tools.bench/validation. === PRIOR: DONE 2026-06-14 pt3 (GAP-ANALYSIS P2 -- lm-7 WINDOW PARITY + DEAD-KNOB HYGIENE; merged feat/memory-window-parity-hygiene -> main @56e75c2; 1665 green). lm-7: MemoryManagerAdapter sizes its all() ring off a new working_window param (forwarded by core/app._build_memory) instead of max_recent_messages(20), so the addressing/cleaner recent buffer matches SessionMemory + SqliteVecMemory (working_window=200); back-compat fallback when unset. Dead-knob hygiene (lm-4/5/6/8): DELETED the confirmed zero-reader keys memory_smart_save/memory_flush_interval_sec/memory_enable_embeddings/memory_persist_assistant/memory_llm_clean (top-level) + meeting_persist (memory block); the meeting_persist getattr was always False (never wired) so the adapter meeting branch is now `if 'meeting' in tags: return` -- behavior-identical, meeting-notes-RAM-only is a HARD §9.7 guarantee. Docs (MEMORY.md drops deleted keys + phantom --memory-no-* CLI flags that never existed; unified_architecture.md meeting=always-RAM-only) updated for current-truth. Verified deleted keys have NO remaining readers + no CLI path. +tests (ring-cap, meeting-routing, working_window forwarding, dead-keys regression guard). NOTE: the broader writer-config block (memory.min_confidence/llm_cleanup/llm_gate/cleanup_model/etc via MemoryWriterConfig) is DORMANT -- _build_memory never passes memory_config to the manager; wiring-or-pruning it + the full 'every memory key consumed' test (lm-8) is a deferred follow-up (touches smart-save behavior). REMAINING P2 (scouted, see docs/session_2026-06-14_sqlite_memory.md): lm-2 continuity (Postgres _summary_head seed + cross-session _load_recent_messages fallback; Wire-3 'Last session' block needs an injection-point call); recall-quality (timestamps on recalled lines CONFLICTS with byte-identical parity -- backends store different wall-clock ts for the same logical add, so needs a parity-safe design; profile-injection decouple from recall_enabled, narrow value+lm-3 precondition); retention/provenance (daily apply_retention schedule, 'forget that' intent, migration 005 tags/channel + ambient RAM-only, record D8); lm-5 episodic (persist assistant finals -- re-add memory_persist_assistant WITH a consumer, or episodes table; lm-3 precondition); the lm-9/aq-4 factor-policy-out refactor + lm-8 writer-config wiring/consumption test. Then P3 routing quality+cost, P4 installer/BargeInDetector/mobile, P5 remote auth. OWNER still owes: rotate Gemini key + D1 history purge + speaker-ID enroll; flip recall_enabled after tools.bench. === PRIOR: DONE 2026-06-14 pt2 (GAP-ANALYSIS P2 START -- PERSISTENT SqliteVecMemory backend, Decision D6; merged feat/sqlite-memory-backend -> main; commits 4e352c3 feat + 974ba78 review-fixes; 1660 green). The third Memory backend behind the one 6-verb protocol, alongside in-RAM SessionMemory + Postgres MemoryManagerAdapter. always_on_agent/sqlite_memory.py: stdlib-sqlite3-only, PERSISTENT (rows survive restart -> cross-session continuity on a desktop WITHOUT Postgres; the Dart/mobile reference). Recall goes through the SAME shared candidate_for_item -> build_block path as SessionMemory (extracted candidate_for_item() in always_on_agent/memory.py as the single source of truth) so it is BYTE-IDENTICAL for identical stored data at ANY size (recall pool tracks max_items; SessionMemory evicts beyond max_items so both consider the same window). Keyword overlap by default; optional injected `embedder` stores float BLOBs + ranks by pure-Python cosine over the recent pool (native sqlite-vec ANN is a future accel). TTL prune + working-window all(); thread-safe (one conn + lock); memory.db chmod 0o600 / dir 0o700 (§9.7 -- persisted post-ASR + vision text is PRIVATE, must not be world-readable); idempotent close. Wired backend=='sqlite' in core/app.py _build_memory (sqlite_path default ~/.speaker/memory.db; degrades to in-RAM on error; default backend UNCHANGED = auto, so opt-in). NO MemoryManager 'factor-policy-out' refactor (deferred lm-9/aq-4 follow-up). Built via scout (6-agent P2 state map) + 15-agent adversarial review (8 findings, ALL fixed: _RECALL_POOL parity break, world-readable db, non-idempotent close, tautological cosine test, + coverage gaps). +25 tests (tests/test_sqlite_memory.py stdlib-only/CI-safe; test_memory_contract _backends()->factory list covers all 3 backends). See docs/session_2026-06-14_sqlite_memory.md. NEXT (rest of gap-analysis P2, all scouted -- see that handoff): lm-2 continuity (seed _summary_head from newest summary + cross-session _load_recent_messages fallback + one-shot 'Last session' block -- Wire 3 needs an injection-point call); recall-quality (timestamps on recalled lines [open]; DECOUPLE profile injection from recall_enabled so durable profile facts inject when episodic recall is off [open, must keep lm-3 sensitivity float]); dead-knob hygiene (DELETE memory_persist_assistant+meeting_persist, add 'every memory config key is consumed' test); retention/provenance (daily apply_retention schedule on writer thread; 'forget that' intent; migration 005 tags/channel + ambient RAM-only; record D8 in target_architecture.md -- gap-analysis already recommends the default); lm-7 window parity (forward working_window to the adapter ring; ambient-vs-conversation caps) + lm-5 episodic (persist assistant finals via the dead memory_persist_assistant knob OR an episodes table -- and/or fork, default-OFF, lm-3 precondition). Then P3 routing quality axis + cost accounting, P4 installer scipy/soxr + BargeInDetector extraction + mobile convergence, P5 remote /token per-principal auth. ALSO carried from pt1: OWNER still owes rotate-Gemini-key + D1 history purge + speaker-ID enroll; flip recall_enabled after tools.bench; the factor-policy-out refactor (lm-9/aq-4). === PRIOR: DONE 2026-06-14 (GAP-ANALYSIS P0/P1 CODE-ACTIONABLE SLICE -- merged feat/gap-analysis-p0-p1-hardening -> main; commits 0ab1ca3 security+lm-1, f24b455 rc-3/4/5/6+sr-2, 417650e review-fixes; 1640 green). Landed every P0/P1 item from docs/review_2026-06-10_gap_analysis.md that needs NO owner key / NO history rewrite / NO live mic+screen. Built via 3 ultracode workflows: 11-agent scout (verified each item's real state -- the backlog line numbers had drifted), 17-agent adversarial review (6 dims -> verify each finding; 9 confirmed + all fixed), focused rc-4 re-verifier. WHAT: P0 -- gitleaks CI gate (.github/workflows/gitleaks.yml + .gitleaks.toml; INCREMENTAL scan so the historical .env leak doesn't red it; full-history audit is the separate pre-release step), SECURITY.md, tools/migrate.py DSN redaction (self-contained -- NOT importing setup_database which sys.exit()s without psycopg = a SystemExit that would have RED-ed CI + crashed `migrate status`; also masks ?password= query form), remote/token_server.py generic 500 detail + logger.exception (was leaking str(exc) incl. LIVEKIT_API_SECRET; ALSO fixed a latent PEP-563 bug where /chat 422'd every call -- hoisted Request into module globals), §9.7 doc reconcile (committed bundles must be PII-free: docs/debugging.md + CLAUDE.md + .gitignore), migrations/002 invalid ADD CONSTRAINT IF NOT EXISTS -> DO-block, migrations/004 HNSW on unconstrained vector -> cast vector(384), .github/workflows/migrations.yml (pgvector service apply+re-apply -- VERIFIED end-to-end incl. rollback in docker). P1 real-time correctness (rc-1/rc-2/lm-3 were already done): rc-5 HANDLED_LOCAL metric + watchdog skip (kills false 'llm stuck' on the no-LLM fast-path; INGEST/KWS/merged were already safe); rc-6/aq-7 bounded SupervisorState histories w/ deque(maxlen) (transcript_log stays a list -- sliced by drivers) + STT_PARTIAL out of event_log; rc-4 cancel_all cancels queued tasks + _start_queued_tasks snapshots under lock AND starts INLINE (fixes the review-caught over-admission past the RESEARCH cap/global ceiling) + start_epoch recheck (closes the residual resurrection window) + _start_task drops pre-cancelled/epoch-stale; sr-2 spoken failure apology on TASK_FAILED (payload carries speak/followup/epoch) + cross-tier fast<->main retry in capabilities.assistant() (distinct-tier + no-audio-emitted guards = no double-speak; addresses mute-on-tier-failure); rc-3 per-utterance GENERATION COUNTER in sherpa.py -- gen-on-enqueue, worker _claim_utterance() skips a stale sentence WITHOUT wiping _stop_speaking (the dequeue->clear race), in-flight synthesis aborts on gen mismatch, a new reply after a barge keeps the new gen so it is NEVER muted (the scout's 'clear-if-not-stale' proposal had a latent mute-bug; gen-on-enqueue avoids it). +30ish tests. NEXT: (1) OWNER still owes -- rotate the leaked Gemini key + D1 history purge + speaker-ID enroll. (2) Watch the new gitleaks + migrations CI gates go green on the push. (3) gap-analysis P2 (memory continuity/SqliteVecMemory/dead knobs -- lm-3 §9.7 precondition is in), P3 routing quality axis+cost, P4 installer scipy/soxr + BargeInDetector extraction + mobile convergence, P5 remote /token per-principal auth. (4) LIVE re-validate barge-in + tier-failure fallback on --engine sherpa (deterministically tested, but touches the real-time path). (5) Carried: flip recall_enabled after tools.bench. See docs/session_2026-06-14_gap_analysis_p0_p1.md. === PRIOR: DONE 2026-06-13 pt2 (PERSISTENT VISUAL MEMORY -- merged feat/visual-memory -> main @d9e99f7; commits b6952c8 feat + d94ec3c review-fixes; 1614 green). The assistant could SEE the screen (ephemeral per-turn multimodal context) but never REMEMBERED it. NEW core/visual_memory.py VisualMemorizer: when screen_capture.enabled+memorize (both DEFAULT-OFF), each new frame -> OFF THE HOT PATH (throttled min-interval + on-change sha1 fingerprint, bounded drop-oldest worker) -> a short LOCAL multimodal caption + an OCR snippet -> stored as a recallable 'vision' memory, surfaced via the SAME smart-recall layer (kind='vision', 'Screen:' label). Ingestion uses a dedicated MemoryManager.add_observation (role='observation'/source='vision') that BYPASSES _extract_profile; clear_observations() purges; vision is excluded from recent-context + the addressing/cleaner windows. get_context_for_llm is now 3 reserved-floor sub-passes (recall/vision/profile) sharing ONE max_tokens. PRIVACY (\u00a79.7): caption uses the BARE LOCAL model only (llm_factory _tag_local_main -> .local_main; app passes it; build_visual_memorizer hard-disables captioning if only a cloud handle is available) -- the adversarial review (20 agents) caught this as a BLOCKER cloud-leak when llm.cloud.enabled+keys; recalled vision floats PRIVATE. Needs mss (capture) + pytesseract/Pillow/tesseract (OCR; degrades to caption-only) + a local multimodal model. NEXT: live-validate on a real screen (display+model); optional extras packaging for mss/pytesseract. See docs/session_2026-06-13_visual_memory.md. === PRIOR: DONE 2026-06-13 (SMART/EFFICIENT MEMORY RECALL -- branch feat/smart-memory-recall-budget, commits 1f0a43c feat + c23f5cc review-fixes; landing to main). Goal: recall must not blow up the LLM context. Replaced 3 stacked blunt char caps (per-item [:150] RAM+PG, total [:600] at injection) + every fixed threshold (cosine>0.6, top-3, overlap>=2) with ONE token budget + a DATA-DERIVED adaptive cutoff. NEW always_on_agent/recall.py (stdlib-only, zero-utils-dep, SQLite/mobile-portable): Candidate, RecallBudget, estimate_tokens, adaptive_cutoff (scale-free DOMINANCE rule: largest gap >= 2x next-largest -- FP-robust, fires at n>=3), collapse (near-dup + summary-subsumes-span; profile exact-dedup only), pack (value-density knapsack measured on the RENDERED block), compress (whole-sentence, query-aware, label-aware, never mid-word), build_block, trim_block_to_tokens. Both SessionMemory (RAM) and MemoryManager (PG) are thin candidate-gatherers over build_block -> byte-identical block (parity gap closed). Profile is a SEPARATE budgeted sub-pass sharing ONE max_tokens with recall (reserved floor so episodic recall can't evict durable facts) -> combined <= budget. Default budget 150 tokens (== old 600-char//4, so enabling recall never raises volume). recall_max_chars kept as deprecated alias; dead recall_min_similarity/recall_max_items removed. Summarize trigger (_estimate_tokens) UNTOUCHED. Recall stays default-OFF (zero live change). Built via 2 ultracode workflows: 16-agent design (map->design->judge->synthesize->critique) + 28-agent adversarial review (20 findings, all real ones fixed). NEXT: (a) flip recall_enabled on after `python -m tools.bench` confirms a favorable token delta; (b) optional follow-up migrations/005 salience+pg_trgm hybrid (deferred, targets the proper-noun/STT-garble confabulation); (c) inject recall on the ReAct/escalated path too (today only assistant.answer). See docs/session_2026-06-13_smart_memory_recall.md. === PRIOR (2026-06-10 session, STILL OPEN): NEXT (priority): (1) OWNER 2-MIN ACTIONS -- rotate the leaked Gemini key (public git history since d32db9f); speaker-ID ENROLLMENT `python -m core --enroll` (gate fail-open until then; the big remaining lever for house-noise/other-voices; if it rejects the owner lower sherpa.speaker_threshold toward 0.4). (2) Validate round 4/5 live: house noise + mutters must stay silent (cleaner guards + noise-aware addressing + unsure_acts=false), real questions still answered; bundle run-20260610-135844 unanalyzed. (3) The remaining live follow-ups in .agents/backlog.md 'follow-ups from the 2026-06-10 LIVE iteration': _speaking-clears-while-FIFO-drains root cause (full-sentence echo mechanism -- the L4 guard masks it); drop assistant replies from the cleaner context (root-cause vs guard); endpoint_min_silence_sec 1.1->0.7 once turn-merge proves out; watchdog false stuck-hints on held/merged turns (rc-5); STT garble = the quality ceiling (mic gain / SenseVoice live tuning); fast-tier shallowness on contextful turns (roadmap P3 quality axis). (4) D1 history purge decision still open (key + voice WAVs in history; WAVs stay until pre-release per owner D2). (5) The 2026-06-10 gap-analysis roadmap P1-P5 (docs/review_2026-06-10_gap_analysis.md). Step-2 echo-floor measurement gate: respected all day (only the ref-delay CALIBRATION changed, no K/margin tuning); resid_floor stability on normalized playback still unmeasured live. === PRIOR: NEXT (priority): (1) OWNER P0 SECURITY -- rotate the leaked Gemini key (aistudio.google.com; it is in public git history since d32db9f regardless of the tip fix), decide D1 history purge + D2 voice-fixture policy (docs/review_2026-06-10_gap_analysis.md), fix .claude/hooks/guard.ps1 line 28 (missing ';' between p/why -> the whole guard fails open). (2) LIVE ON-MIC VALIDATION (plan step-2 gate + this session's fixes): python -m core --engine sherpa -- (a) resid_floor should sit ~1-2x not 20-90x (normalized playback), (b) normal-volume talk-over must cut without a shout / no self-interrupt (persistent-chart fix is trace-validated; owner requirement is live), (c) mid-thought pauses should MERGE (watch 'merged held final' logs) not get answered per fragment -- if it holds too long lower turn_merge.hold_sec, if it still answers fragments add words to hold_endings, (d) 'start again' must not resurrect the old topic. ONLY tune barge knobs after (a) passes. (3) Windows aec_ref_delay_ms echo_probe recalibration (carried P0, needs mic; config.local.json still has stale 19ms). (4) Adopt the gap-analysis roadmap: P1 leftovers (rc-3 TOCTOU, rc-4 queued-task resurrection, sr-2 spoken failure apology, rc-5 watchdog false 'llm stuck', lm-1 migration 002), then P2 memory continuity (cross-session summary head, SqliteVecMemory, dead knobs), P3 routing quality axis + cost accounting, P4 installer/profiles/BargeInDetector extraction/mobile convergence. Linux box: plan step 4 (flip asr_final_backend '' -> sense_voice in ITS config.local.json; guard + min_sec already landed). === PRIOR: DONE 2026-06-09b (RECORDED REAL-VOICE REPLAY HARNESS; merged feat/recorded-voice-replay-harness -> main @830e086; local 1453 passed/15 skipped, CI self-skips). Extracts the OWNER'S actual utterances from logs/runs/run-*.wav and replays them as MIC input through the REAL pipeline (VAD->endpoint->sherpa ASR->brain->TTS) headless, so capabilities are regression-tested against his real speech with NO live talking ('simulate real usage'). PIECES: tools/extract_voice_clips.py (slices utterance windows to the gitignored logs/fixture_audio/ via core.engines.file_replay.load_waveform; NEVER git-adds voice audio -- privacy); tests/replay_voice_driver.py (shared seam: feeds a clip through FileReplayEngine + core.app.build_runtime + EchoLLM for determinism, returns asr_final/response/latency; SPEAKER_LIVE-only barge-overlap path via the live_session inject machinery); tests/replay_recorded_voice_test.py (real_model/recorded tier: 6 clean utterances reproduce at >=0.4 word-overlap, a run-235431 multi-turn convo runs through the real brain, SPEAKER_LIVE barge cut); tests/fixtures/recorded_voice_manifest.json (COMMITTED text-only windows -- run/start/end/expected_text). Built via a 4-phase ultracode workflow; the workflow's first automated pass MIS-SEGMENTED (used summary.json at_sec = runtime clock, NOT the WAV timeline -> clips held the wrong audio, all 0.0 overlap) -- I re-derived the windows via faster-whisper word-timestamps + per-clip REAL-sherpa-ASR verification, dropped 3 starved-mic + 2 non-reproducing clips, and gated the hardware-touching barge test behind SPEAKER_LIVE (it opened the real ALC285 otherwise; deterministic barge coverage already lives in tests/test_barge_*). VERIFIED clips reproduce: 'Can you tell me a story' (1.0), 'Tell me a long story' x2 (1.0/0.8), 'Stop speaking' (1.0), 'Hey hey' (1.0), 'are you listening to me' (0.6). USAGE on a machine with the recordings: `python -m tools.extract_voice_clips` then `python -m pytest tests/replay_recorded_voice_test.py -q` (self-skips without sherpa models OR extracted clips). ===> NEXT: optional -- run the SPEAKER_LIVE barge-overlap test live, expand the clip corpus, and the still-open barge LIVE re-validation (below). === PRIOR: DONE 2026-06-09 (BARGE-IN WINDOWED-SUSTAIN FIX + DETERMINISTIC TEST SUITE; merged test/barge-in-capability -> main @0a1cdf5, 1445 green). LIVE DEBUG SESSION on the Linux box (ALC285 laptop mic+speaker, AT2020 USB mic UNPLUGGED): owner reported barge-in 'i need to scream loud to stop'. ROOT CAUSE (run-20260609-203236, two layers): (1) MIC STARVATION -- far-field ALC285 at input_gain=2.0 captures the user at raw RMS ~0.002-0.07 (vs historical real-barge ~0.4), repeated 'input is ~silent (avg_rms=0.00008)' warnings; the 2026-06-08 'feels good, no shout' result was on the CLOSE AT2020 mic, absent today. (2) DECISION-LAYER (the fix): AdaptiveDTD FIRED on the NORMAL talk-over (turn-2: 3 of 5 blocks) but the leaky capture-loop integrator (voiced_run += 0.1 on fire, *= 0.5 on miss, fire at barge_in_min_speech_sec) starved on the flicker (peaked ~0.15s < 0.3s); only the 10x shout (raw 0.0704) sustained a cut. No decay reaching a 3-block (0.3s) threshold can ever fire the turn-3 PRE-SHOUT case (only 2 DTD fires), so the mechanism had to tolerate flicker AND the threshold had to drop. FIX: replaced the leaky accumulator with core/engines/_dtd.BargeSustain -- a BOUNDED windowed sustain that cuts when >= barge_in_min_speech_sec of eligibility lands within the trailing barge_in_sustain_window_sec (NEW SherpaConfig knob, default 0.5; min_speech default stays 0.2 = 2 of the last 5 blocks). Converts intermittent fires into a cut (recorded turn-2 cuts at idx 11, NO shout) AND makes echo self-interrupt STRUCTURALLY impossible (bounded window -> a sporadic single-frame echo leak over a long reply can never accumulate). Machine-local config.local.json lowered barge_in_min_speech_sec 0.3->0.2 (gitignored; config.json default was already 0.2 = shipped fix). The integrator is now an importable class so the test driver drives the REAL BargeSustain (closed the one 'mirrored, not imported' caveat). ALSO built (via a 5-phase ultracode workflow) a DETERMINISTIC, audio/model-free BARGE TEST SUITE from the live failure: tests/barge_fixtures.py replays the recorded per-frame (raw,resid,incoh) DTD trace through the REAL AdaptiveDTD + REAL BargeSustain; tests/test_barge_{trace_replay,requirement,echo_must_not_fire,gate_conversion}.py + 8 BargeSustain unit tests in test_dtd.py; evidence committed under tests/fixtures/barge_in/ (frames.json + trimmed wav + trace). The 4 owner-requirement tests (were strict-xfails pinning the bug) are now PASSING guards that go red if barge-in regresses. Two commits: bedc98b (suite) + 7966d70 (fix). ===> #1 NEXT (NEEDS THE MIC -- owner chose 'merge now, validate later'): LIVE RE-VALIDATE -- `python -m core --engine sherpa --input-device 'ALC285 Analog' --output-device 'ALC285 Analog'`, talk over a reply at NORMAL volume, confirm it CUTS without a shout and does NOT self-interrupt (deterministic proof is strong -- the normal talk-over already fired the DTD -- but the owner requirement is a LIVE behavior). ALSO (problem #1, for STT robustness, not just barge): raise the ALC285 capture level / input_gain so a normal voice reaches raw~0.1+, or plug in the AT2020 USB mic. GOTCHA seen this session: a transient PortAudio enumeration race can start the core process with NO input devices (sd.default.device = [-1,0], empty input list) -> 'No input device matching' + 'Error querying device -1'; a plain relaunch fixed it (a _terminate/_initialize cycle also clears it). === PRIOR: DONE 2026-06-08g (CONVERSATION CONTEXT ON ESCALATED TURNS; merged feat/conversation-context-to-escalated-path -> main @6b39e2e, 1422 green). Owner asked to 'enhance the memory... last sentences + capabilities + what is prior he spoke, with the good version of the speech to text.' Investigation: the whole pipeline ALREADY exists + uses the good (agreement-guarded) final at zero latency -- core/conversation.py recent-turns block (6 turns/800 chars), core/persona.build_system_prompt capability list, core/cleanup.py optional LLM cleaner (OFF). Owner (AskUserQuestion) chose: keep cleanup OFF, keep depth 6, keep anonymous. So the real gap (not a config toggle): the recent-conversation block was built ONLY on the one-shot answer path, AFTER the escalation early-return -- so an ESCALATED ReAct turn ('explain that') got a contextless query. FIX: build the block ONCE before the escalation branch, publish into context['recent_conversation'] (RECENT_CONVERSATION_KEY); always_on_agent/react.py prepends it to the plan + final prompts. \u00a79.7: sensitivity floats over the included prior turns BEFORE capability_context.set(). One-shot path byte-identical (same block/float/ingest-order/continuation-suppression). +6 tests incl. an end-to-end real-planner contract pin. Adversarial-reviewed SHIP. Handoff: docs/session_2026-06-08_conversation_context_escalated.md. NOTE: if the owner meant longer-term cross-session RECALL (not this-conversation short-term), that's memory.context_for_llm / recall_enabled (default off) -- a separate lever. === PRIOR: DONE 2026-06-08f (ECHO-FINAL CASCADE FIX; merged fix/echo-final-cascade -> main, 1416 green). The owner reported it 'shutting down again' = the open-speaker self-interrupt + 'two outputs one after another' cascade from the 2026-06-08e Windows live run. Built via a Map->Design->Build->Verify fan-out workflow (the workflow's build/verify agents got TRANSIENT-rate-limited mid-run; B1's agreement_guard + tests had already landed on disk, so the engine layer was finished + verified in-loop). ROOT CAUSE (code, device-independent): the final-dispatch seam had NO energy floor -- the recognizer turned the assistant's own residual echo/ambient into 'finals' (avg_rms ~0.005-0.018 vs ~0.2-0.4 real) and dispatched them, each spurious reply's echo self-interrupted, the cancelled tail's echo became the next garbage final -> runaway. FIX = 3 device-adaptive, additive, off-switchable layers (NO fixed magic-number thresholds; every bar RELATIVE to a LEARNED per-device floor): L1 _final_above_floor (core/engines/sherpa.py) gates the seam on rms(seg) >= max(_ambient_rms quiet floor, _playback_floor_rms echo floor) * final_floor_margin_db (6.0 in config.json; dataclass 0.0 = off; fails OPEN until a floor is learned, so it only bites on AEC/open-speaker configs where one IS learned); L2 _in_post_speaking_refractory + _last_speaking_end stamped at both _speaking->clear sites (which fire per-REPLY not per-sentence, guarded by `if self._play_q.empty()`), ORed into the BARGE debounce ONLY (never the final seam, so a real barged-in request is never dropped), barge_in_refractory_sec=0.5; L3 agreement_guard (core/asr_text.py, B1) wired into _final_transcribe demoting short-clip SenseVoice hallucinations ('BEING'->'I.') back to the streaming final (keys on clip LENGTH not energy). +30 tests (encode the real failing RMS numbers); _should_act_on_final UNCHANGED so the 9 input-gate tests are byte-identical. CAVEAT: FileReplayEngine.replay_samples calls on_final() directly and BYPASSES the seam, so the headless WAV replay can't exercise L1/L2/L3 (the unit tests are authoritative); it DID run clean + showed the ~31 raw garbage finals the gates target. ===> #1 NEXT (P0 remainder, NEEDS THE MIC): the FIRST self-interrupt itself -- the Windows AEC mis-calibration (stale pre-FIFO aec_ref_delay_ms). `python -m tools.echo_probe` (echo-only) on the Windows box -> pick the ERLE-max aec_ref_delay_ms (don't assume 0), raise dtd_k if echo-only D approaches K=5.0, target self_interruptions=0; replay run-20260608-181250.wav through echo_probe to iterate without re-talking. The cascade fix makes this miscalibration NON-catastrophic now. Then live-tune final_floor_margin_db (6.0) / barge_in_refractory_sec (0.5) -- watch the echo_floor_rejected_final metric in run bundles. Handoff: docs/session_2026-06-08_echo_final_cascade_fix.md. === PRIOR: LIVE SESSION 2026-06-08e (run-20260608-181250) -- FIRST live --engine sherpa run on the WINDOWS side. FINDING: it SELF-INTERRUPTS on its own open-speaker echo (every barge-in fired at avg_rms ~0.005-0.018 = echo, not a ~0.2-0.4 voice), and each cancelled tail's echo -> a SenseVoice-hallucinated short final -> a new response -> a runaway 'two outputs back-to-back' cascade. NOT a regression from this session's code (think/routing/polish don't touch AEC/barge/ASR) -- the Windows config.local.json still has the PRE-FIFO aec_ref_delay_ms=19 + asr_final_backend=sense_voice; barge-in was only calibrated on the LINUX side (validated ref_delay=0). ===> #1 NEXT (P0, needs the mic): re-calibrate barge-in for the Windows box -- `python -m tools.echo_probe` (echo-only) to pick the ERLE-max aec_ref_delay_ms (don't assume 0), raise dtd_k if echo-only D nears K=5.0, target self_interruptions=0; then disable/guard SenseVoice (asr_final_backend='' or the core/asr_text.py agreement-guard) to break the cascade. Replay run-20260608-181250.wav through echo_probe to iterate without re-talking. FULL ANALYSIS + plan: docs/session_2026-06-08_live_session_self_interrupt.md; P0 in backlog.md. GOOD NEWS validated live: the think=false latency fix WORKS (story 'Give me a long story' -> main tier -> first sentence ~3.4s, not 9s); prosody endpointing is active (turn-taking unreadable under the self-interrupt chaos -- re-assess after the P0 fix). === PRIOR: DONE 2026-06-08d (P2 ROUTING POLISH via the p2-routing-polish fan-out -- 3 file-disjoint parallel implementers; merged to main, 1407 green). 6 of the 7 audit P2 items: (a) HedgeLLM.shutdown WARNs survivor-thread count (speaker.llm.hedge) so the known leak is visible; (b) WINNER_SELECT_BUDGET_FLOOR 30s->10s; (c) tier markers are \\b-anchored ('show me the time' no longer hits 'how'; multi-word still match) + compose/draft/'write an'; (d) the ESCALATED ReAct + research.local paths now _enrich_context + publish capability_context (try/finally reset, no cross-turn leak) so SensitivityRouterLLM routes them correctly; (f) LearnedRouter build-path test; (g) cost_order doc-truth (config.json comment + docs citation). Item (e) tier-aware load_fraction DEFERRED -- coupled to live_routing (audit #4). NOTE one agent-written test needed fixing: a hung CLOUD worker must actually be LAUNCHED to be a survivor (hedge launches clouds lazily), so the test gives local a first_token_delay=0.1 so the cloud is kicked before local wins. ===> #1 NEXT unchanged = audit #4 (activate live_routing+cost_order on the CLOUD profiles + bench evidence, needs cloud keys) + phase-1 carryover (PROSODY endpointing ACTIVATED on Windows config.local.json, needs owner LIVE tuning; SenseVoice agreement-guard needs recorded clips). === PRIOR: DONE 2026-06-08c (SMART-ROUTING PHASE-2 HARDENING; merged feat/smart-routing-phase2-hardening -> main @f7bf559, 1398 green). 6-dimension fan-out AUDIT of smart routing (28 findings, 19 confirmed after adversarial verify -- 9 plausible-but-wrong dropped). VERDICT: smart routing is largely HEALTHY + fail-safe (no P0, no active \u00a79.7 leak). Through-line = DORMANT intelligence (live_routing/cost_order/capability_router built+tested but off) + one narrow PII gap. LANDED 3 verified fixes: (1) core/sensitivity.py PII fail-safe (lowercased ASR 'what is john salary' defeated the case-SENSITIVE name+money rule -> added case-insensitive comp-word salary/wage/income/paycheck/pay stub/net worth/bonus -> PRIVATE); (2) core/routing.py _preset_cost_key host_rank OUTERMOST (CN sorts after US/unknown -- fixes CN-floats-ahead + OpenRouter-sinks-below-CN; latent until cost_order enabled); (3) config.json capability_router ON for desktop_gpu_4090 (shipped device inherited enabled=false while base 'desktop' had it on). ===> #1 NEXT (audit #4, DEFERRED -- needs cloud keys to validate): activate live_routing:true + cloud.cost_order:true on the CLOUD profiles (cpu_laptop/phone_lite/macbook; keep desktop/4090 off, local 12b fast) + add a tools/bench (or replay) smoke that emits the chosen chain order + asserts cost_order lowers TTFT and the live nudge shortens the hedge under a high-load snapshot (first measured proof). cost_order fix above unblocks it. Plus P2 polish (daemon-thread-leak WARN, WINNER_SELECT_BUDGET_FLOOR 30s->~10s, word-boundary tier markers, capability_context on escalated ReAct/research paths, tier-aware load_fraction, LearnedRouter test, doc-truth fixes) -- full list in .agents/backlog.md 'Smart routing - phase-2 audit'. FULL HANDOFF: docs/session_2026-06-08_smart_routing_phase2.md. ===> ALSO STILL OPEN from phase-1 (owner improvements #2/#3): PROSODY ENDPOINTING is ACTIVATED on the Windows config.local.json (endpoint_detector=prosody) -- needs the owner's LIVE tuning (talk with mid-thought pauses; raise endpoint_incomplete_threshold toward 0.6 if it still replies on a pause). SenseVoice agreement-guard (#3) still open (needs real recorded clips). === PRIOR: DONE 2026-06-08b (STREAMING-TTS LATENCY -- owner improvement #1; merged fix/llm-think-off-tts-latency -> main @b2cddc4, 1395 green). FULL HANDOFF: docs/session_2026-06-08_streaming_tts_thinking_latency.md (read first). HEADLINE: the streaming chain was NEVER the problem -- per-sentence emit -> TTS_REQUEST bus -> engine.speak -> playback is fully incremental and works. The real cause of 'a long story feels like it waits for the whole answer' is that the MAIN tier (gemma4:12b) is a REASONING model: it streams a silent chain-of-thought into a separate `thinking` field BEFORE any `content` token, and OllamaLLM.stream only yields content -> ~9s of dead air for a STORY (which needs no reasoning). MEASURED on gemma4:12b (warmed, 100% GPU): content first-token 9.1s, first spoken sentence 9.5s, full answer 11.2s. FIX: threaded a `think` param through core/llm.py OllamaLLM (top-level chat arg, sent only when set -> non-reasoning models / older Ollama unaffected) + default it OFF in core/llm_factory.build_llms (llm.think=false in config.json, overridable). AFTER (production factory path): first-token 1.8s, first sentence 2.4s, full 6.1s -- a 7s cut; streaming TTS now genuinely helps (audio at 2.4s vs full 6.1s). NOTE: gemma3:4b (fast tier) is NOT a reasoning model (think=false is a graceful no-op). ===> #1 NEXT (owner improvement #2 -- ACTIVATED on the WINDOWS box 2026-06-08b, NEEDS THE OWNER'S LIVE TUNING): PROSODY ENDPOINTING (Smart Turn v3). Owner clarification: this does NOT replace SenseVoice STT -- it's a separate WHEN-is-the-turn-done layer (decides turn boundaries, transcribes nothing), so it CANNOT lower transcription accuracy; if anything it helps by not truncating mid-thought. NOW ON in config.local.json (gitignored, per-machine): endpoint_detector=prosody + endpoint_prosody_model=<abs path to smart-turn-v3.1-cpu.onnx> + endpoint_incomplete_threshold raised 0.3->0.5 so mid-thought pauses (low score) trigger the bounded HOLD; verified the runtime builds ProsodyTurnCompletionDetector (needs_audio) from the merged config; the detector loaded+ran a real human clip -> 0.9134 (complete), thin audio -> 0.5. LIVE-TUNE: run `python -m core --engine sherpa`, talk with mid-thought pauses; if it still replies on a pause raise endpoint_incomplete_threshold toward 0.6; if it holds too long lower it; to cut post-speech latency lower endpoint_min_silence_sec (1.1 -> ~0.5) once happy. Validation knobs from 2026-06-01: complete turns 0.74-0.98, incomplete 0.01-0.56. The model is HUMAN-AUDIO only (flat ~0.97 on TTS) -> synthetic harness can't validate; needs real speech. On OTHER machines it's still OFF (flip the same keys in that machine's config.local.json). The model is HUMAN-AUDIO ONLY (flat ~0.97 on TTS) so the synthetic harness CANNOT validate it -- needs real speech. (B) SenseVoice agreement-guard for STT (owner improvement #3): currently DISABLED (asr_final_backend='') because the 2nd pass hallucinated SHORT clips ('SO'->'I then.'). Proper fix = new core/asr_text.py token-agreement helper gating _final_transcribe (core/engines/sherpa.py:1098). SUBTLETY found this session: a naive overlap guard would WRONGLY reject the legit garbled-correction case the 2nd pass exists for ('Ario der'->'are you there', low overlap) while the hallucinations are the SHORT-clip cases -- the discriminator must key on length/short-clip risk, NOT pure overlap. Needs real recorded STT clips (hallucinations + good corrections) to tune -> perceptual, validate live. (C) Re-enable input_gate.enabled=true for always-on use (config.local.json, currently false for direct-reply testing). (D) housekeeping (carried): core/runlog.py keeps 20 bundles but ~50 committed -> every run shows logs/runs deletions in git (bump SPEAKER_KEEP_RUNS or trim committed bundles). === PRIOR: DONE 2026-06-08 (DEVICE-ADAPTIVE BARGE-IN WORKS on the open laptop speaker; merged fix/barge-raw-mic-and-latency -> main, 1369 green). FULL HANDOFF: docs/session_2026-06-08_device_adaptive_barge_in.md (read first). Owner confirmed 'barge feels good now' -- NORMAL-volume talk-over interrupts reliably on the bare ALC285 mic+speaker, no shout, no self-interrupt. SOLUTION: AdaptiveDTD (core/engines/_dtd.py) -- fused self-calibrated z-scores (raw/residual/coherence) from per-device echo-only control charts, fire on weighted SUM D>K, NO fixed margin. DECISIVE FIX was the FIRING LOGIC: dtd_confirm_frames=1 (per-frame) + capture-loop LEAKY integrator -- a real talk-over scored D=90-130 but flickered, so the old 3-consecutive rule discarded it. Weights (raw 0.2, resid 1.0, coh 0.0): z_resid is the discriminator (user voice isn't in the reference -> AEC can't cancel it -> lands in residual). Also: fast_model=gemma3:4b (latency 8-12s -> 0.4s), persona answer-directly, endpoint_high_confidence_floor=0. ===> #1 NEXT (owner-requested 2026-06-08): (A) STREAM TTS for long answers -- a long story feels like it waits for the whole LLM answer; _stream_and_speak already emits per-sentence so investigate the sherpa-path first-token latency (gemma4:12b main tier) / confirm incremental emit->TTS. (B) SMARTER ENDPOINTING -- don't reply on the user's mid-thought pauses; switch endpoint_detector to 'prosody' (Smart Turn v3 on disk) + confidence floor. (C) SenseVoice agreement-guard for STT. NOTE machine-local config.local.json: input_gate.enabled=false (re-enable for always-on), asr_final_backend='' (sense_voice reverted -- hallucinated). === PRIOR: DONE 2026-06-07 (BARGE-IN COHERENCE-PRIMARY + GEMMA 4 ON LINUX; merged feat/barge-in-coherence-primary -> main @fe0617b, then v2 @377d10e, 1357 green). v2 LIVE-MEASURED on the open ALC285: coherence-ALONE self-interrupted (echo incoherent ~0.88 overlaps voice); FIX = coherence 'user' verdict must ALSO clear the post-AEC residual floor (orthogonal: AEC kills echo ENERGY not its incoherence). Measured 0 self-interrupts across 5 runs at full volume. ===> #1 NEXT: confirm a REAL talk-over STILL fires (HUMAN, can't be machine-tested) -- `python -m core --engine sherpa`, talk over a long reply, must cut <~1s; if missed lower barge_in_residual_margin_db (10.0). Also re-confirm on the AT2020 USB mic (was unplugged). Full handoff: docs/session_2026-06-07_barge_in_coherence_primary.md. FULL HANDOFF: docs/session_2026-06-07_barge_in_coherence_primary.md (read first). HEADLINE: open-speaker barge-in is NOT a hardware limit -- it was an architecture bug. The live trigger was a LOUDNESS gate on the post-AEC residual (no clean operating point on a nonlinear speaker); coherence (the volume-independent user-discriminator) was OFF and, when on, fed the post-AEC residual (AEC removes the correlation it keys on -> misread cancelled echo as 'user' = the self-interrupt). FIX: coherence fed the RAW pre-AEC mic is now the PRIMARY trigger (True=fire/False=reject/None=fall through); level gates demoted to a None-fallback. Adversarial review found+fixed a control-chart STARVATION blocker (baseline froze at provisional 0.5 on persistently-over-threshold nonlinear echo -> warm-up seeding learns the true echo floor). Compute-stop leak fixed (cancel now closes the Ollama stream so the server stops generating at the barge point, not at GC). Calibration params (warmup_frames/sigma_k/var_alpha/baseline_alpha/provisional_baseline) plumbed to SherpaConfig. Gemma 4: Ollama 0.30.6, gemma4:12b both tiers in config.local.json device_profiles, model_probe 4/4+vision. ===> #1 NEXT (needs the MIC): LIVE-VALIDATE barge-in on the open ALC285 -- the raw-mic-coherence premise on the nonlinear speaker is theoretically sound + unit-validated but UNVERIFIED on hardware. Run `python -m tools.echo_probe` (echo-only -> self_interruptions=0, read learned baseline/margin), `python -m tools.interrupt_suite`, then `python -m core --engine sherpa` (talk over a long reply: MUST cut, MUST NOT self-interrupt). Tune w/o code: coherence_warmup_frames^, coherence_provisional_baseline^, coherence_confirm_frames^, lower barge_in_min_speech_sec (0.8) for a snappier cut. ref_delay stays 0 until a fresh echo_probe ERLE sweep says otherwise. ALSO: core/runlog.py keeps 20 run bundles but ~50 are committed -> every run dirties git (bump SPEAKER_KEEP_RUNS or trim). === PRIOR: DONE 2026-06-05 pt3 (IMAGE-CONTEXT FEED; merged feat/image-context-feed -> main @d53a2db). core/capabilities.py forwards images (context['images'] per-turn OR ambient image_provider()) -> stream(images=), forces main/multimodal tier (fast 1b can't see images), floats sensitivity PRIVATE (\u00a79.7). VoiceRuntime.set_current_frame(image)/clear_current_frame() let a HOST machine feed the current frame ambiently. Tests in tests/test_core_multimodal.py. REMAINING (user's future): the frame SOURCE (screen-grabber/camera that calls set_current_frame on a cadence). === DONE 2026-06-05 pt2 (TESTING UNIFICATION; merged chore/unify-testing -> main @e930b92, 7 commits, 1350 green). FULL HANDOFF: docs/session_2026-06-05_testing_unification.md. The suite is now ONE marker taxonomy -> 4 tiers: Tier0 unit/logic (no marker = default CI gate), Tier1 slow/e2e, Tier2 real_model (real weights over FIXTURES, no sound, self-skips w/o models), Tier3 live_output (REAL speakers, DOUBLE-GATED behind marker + SPEAKER_LIVE=1). Single entry: `python tools/run_tests.py unit|real_model|live|...` (fast=alias of unit; live preflights tools.live_session --check + sets SPEAKER_LIVE=1 -- the only stage that makes sound). perf.yml gained a real_model_tests job (downloads sherpa models, runs -m 'real_model or recorded'). New coverage: interrupt-race/failure-cascade/multimodal (Tier0), asr_live_quality (real_model), live_output smoke. Docs: docs/testing.md rewritten. OPEN (priority): (1) MULTIMODAL CAPABILITY GAP (real bug, backlog P1) -- core/capabilities.py never reads context['images']; a 'describe this image' turn silently drops the image (LLM clients DO forward images=, now tested). Fix: thread image source -> turn context['images'] -> stream(images=). (2) optional pytest.ini tidy (smoke/dev/audio/backend still declared-unused). === EARLIER 2026-06-05 pt1 (VOICE DEMO, separate handoff docs/session_2026-06-05_voice_demo_live_validation.md): barge-in + smart-routing live demo via tools.live_session --inject; 2 landings @1335. FULL HANDOFF: docs/session_2026-06-05_voice_demo_live_validation.md (read first). HEADLINE: the demo surfaced + fixed 5 bugs. (1) ROUTING/answering-layer: 'explain X step by step' escalated to the ReAct tool planner -> hit the 3-entry STUB corpus (always_on_agent/capabilities.py pipecat/wyoming) -> parroted 'Wyoming is a protocol...' junk. FIXED in always_on_agent/react.py (dropped 'step by step' marker + FINAL falls back to own knowledge). (2) inject-mode latency was SILENTLY null (driver _NullOutputStream only had write(); engine PULLs via PortAudio callback) -> now drives the callback. (3) latency dropped on a racy TTS stamp -> _consume_latency pairs on LLM_FIRST_TOKEN. (4) 16GB-VRAM handling: _free_llm_vram (evict heavy >8GB sets only) + warm-wait 120s; (5) --inject disables AEC (no echo) -> barge 2/2 not 1/2. MACHINE CONFIG (config.local.json, gitignored): gemma3:12b CANNOT coexist with a 2nd tier + sherpa on 16GB (500s/bad-allocs); gemma3:1b fast gives GARBAGE ('7x8'->'Seventeen'); settled on BOTH tiers=gemma3:4b (correct answers, reliable). Pulled gemma3:1b. MEASURED (inject, 4b): first-audio ~2.4-2.6s (endpoint ~1.1s + LLM ~1.2-1.4s + TTS ~50ms); routing splits fast/main correctly; barge 2/2 on 3 scenarios; add-on merge works. STILL OPEN (priority): (1) HONESTY GAP -- assistant confabulates live data it can't fetch (made up a Paris weather forecast); persona/catalog should decline weather/news/time with no tool. (2) search.local/web.search are a 3-entry STUB -- wire SearXNG (web_search config) or gate the ReAct planner off. (3) live-session harness LEAKS RAM (17 fresh runtimes in 1 process -> sherpa bad-allocs after ~4-5 scenarios); run --suite in SMALL BATCHES or make each scenario a subprocess. (4) short-barge (~0.4s 'Stop.') still rejected (barge_in_min_speech_sec=0.4). === PRIOR 2026-06-03 (voice pipeline STT/barge, for context) ===: DONE 2026-06-03 (voice-pipeline live session; 5 landings on main, all green @1322). FULL HANDOFF: docs/session_2026-06-03_voice_pipeline_stt_barge.md (read it first). HEADLINE: STT was garbled because the SenseVoice offline 2nd-pass was SILENTLY OFF (model never downloaded) -> every final was raw streaming-zipformer ('Are you there'->'Ario der'). FIXED by `python -m tools.setup_models --sense-voice` (machine-local model, gitignored -- EACH MACHINE must download it); the loader now WARNs if the backend is set but the model is missing. Also landed: addressing prompt reframe (answers questions again), DTLN onnxruntime thread-bound (aec_num_threads=1 -- fixed CPU 90-100% pegging -> deaf mic + frozen turns), CONFIRM-gate ('yes' answered instead of dead-ending), real_usage EMPTY/inventory. STILL OPEN (continue here, priority order): (1) BARGE-IN talk-over still flaky on the open nonlinear laptop speaker -- DTLN cancels echo for ASR but SUPPRESSES the user's voice during double-talk so the residual-floor gate rejects quiet barges. The real lever: aec_ref_delay_ms is pinned 19ms but the REAL acoustic delay is ~260ms (coherence delay= logs 258-337ms) -- CALIBRATE with tools/echo_probe.py + gate DTLN to run only while self._speaking (sherpa.py ~1161 engage gate has no speaking guard). Or trigger barge on the coherence detector (sees the user) not the DTLN-suppressed residual. (2) ENDPOINTING cut the user off mid-thought -- stopgapped to endpoint_min_silence_sec=1.1/high_confidence_floor=0; proper fix = endpoint_detector='prosody' (Smart Turn model ALREADY on disk at pretrained_models/sherpa/smart_turn/). (3) fast-tier hallucination; (4) a 'stop' KWS fast-path (kws not built). GOTCHAS: the user's C: disk was FULL (0 bytes) -- it CORRUPTED config.local.json to 0 bytes mid-session (I reconstructed it; it's gitignored + machine-local, see handoff for the working tuning values); free disk before live runs. faster-whisper installed in venv for tools/transcribe_run.py ground-truth (cache deleted to free space, re-downloads on use). === PRIOR SESSION (2026-06-03 barge-stop, for context) ===: (A) d53cac7 fix(barge): RC-2 -- stop_speaking() now AUTHORITATIVELY clears _speaking itself instead of leaving it to the playback worker's epilogue; if native tts.generate() wedges the worker never clears _speaking and the assistant goes DEAF for the rest of the session (capture loop keeps continue-ing past ASR; observed speaking=True ~15s after a 2s reply -> 'playback thread did not exit within 1.0s'). Cut path now clears _speaking + re-arms the one-per-run barge latch + resets echo/far-ref/AEC (all idempotent). Regression test = test_stop_speaking_clears_speaking_and_relatches_on_the_cut (wedged-worker case). RC-5 (observability only, no gate change): when VAD hears SUSTAINED speech during playback but the barge gate keeps rejecting it (and no barge fired this run), log once/episode + emit barge_in_rejected metric so 'user talked over but nothing fired' shows in the run bundle. (B) tools/transcribe_run.py -- timestamped ground-truth transcript (faster-whisper int8 CPU) of a run-bundle WAV to line up against the run log's ASR/barge events. (C) chore(logs) 57354e4 -- pruned stale 2026-06-02 bundles, added 2026-06-03 (incl. run-20260603-101952, a silent-mic capture failure kept as a record). NEXT: RC-5 has NO dedicated test (rejection branch is deep in _capture_loop, no light harness) -- add a capture-loop/replay test if it matters. P1 live-hardware story UNCHANGED from 2026-06-02: open-speaker barge-in is a HARDWARE LIMIT (post-AEC residual echo overlaps a normal voice, no clean operating point); reliable path = HEADPHONES. RC-2 removes the DEAFNESS failure mode that was confounding live tests -- re-run `python -m core --engine sherpa`, talk over a long answer, use the new barge_in_rejected metric to tell 'gate rejected me' from 'engine went deaf'. run-20260603-101952 was a SILENT MIC (avg_rms=0.0) = the known intermittent AT2020 USB-mic problem, not a code bug; re-seat/re-select (python -m sounddevice) before the next live session. Prior 2026-06-02 FIFO/AEC context: see docs/session_2026-06-02_playback_fifo_aec_alignment.md. (1) 554b300 -- recovered the PC-interrupted playback rewrite: TTS flows through a callback-driven OutputStream + bounded PlaybackFIFO so the AEC far-end reference is teed from the audio callback at the TRUE playback position (far->near lag now inside DTLN's +/-60ms); barge-in/shutdown flush the FIFO. Adversarial-reviewed (6 minor, 0 blockers); abort/flush contract documented (a true rollback is unsound under concurrent flush/drain), direct _audio_cb tests, pinned notify wake-path. (2) 088e055 -- the interrupt is now 'a bit slower, higher confidence': EchoCoherenceDetector requires coherence_confirm_frames (default 2) CONSECUTIVE over-threshold frames before firing, so a one-off nonlinear-echo spike can't self-interrupt; identity-free (speaker-ID untouched, stays a separate feature). NEXT (P1, needs the mic): AEC on-hardware -- enable aec in config.local.json, calibrate aec_ref_delay_ms + measure post-AEC ERLE with tools/echo_probe.py now that the far-end ref is aligned; live-tune coherence_confirm_frames (2 vs 3) on a real talk-over; then Smart Turn v3 live floor A/B. Latent real-time item in backlog: move coherence note_playback off the audio callback (lock-free SPSC) before raising coherence_ring_ms. (3) AEC CALIBRATION (tools b5ee64a): the FIFO arch fixed the DTLN far-ref sync -- tools.echo_probe ERLE sweep shows cancellation PEAKS at aec_ref_delay_ms=0 (~10-16 dB live) vs 0.8 dB at the old 94ms; built tools/interrupt_suite.py (mic x strategy sweep) + ERLE/override flags in echo_probe. OPEN-SPEAKER BARGE-IN NOW WORKS (self_interruptions=0): config.local.json set to aec dtln ref_delay=0, identity gate OFF (speaker_gate_input=false, interrupt is identity-free), coherence OFF (conflicts with AEC), aec_relaxed_margin_db=6, barge_in_min_speech_sec=0.6. (4) FIRST live test MISSED the real barge -> root cause: _looks_like_user gated the residual vs playback_level (required user LOUDER than the speaker -- wrong post-AEC). FIXED (b33f525): post-AEC barge now gates on loudness_admits(residual vs _ambient_rms) -- echo-only residual ~0.02, real barge ~0.4 (~26dB above floor). Tuned to self_int=0 at input_loudness_margin_db=15 + barge_in_min_speech_sec=0.8. STILL UNVERIFIED (needs a human): that a REAL barge now fires -- run `python -m core --engine sherpa`, talk over a long answer (should cut off after ~0.8s). The residual math says it WILL fire (0.4 >> ambient+15dB ~0.11). If MISSED: lower min_speech->0.5 / loudness margin->12. (5) ITERATION 3 (e04bb8d): live test had GARBLED TTS (wrong speed/pitch) + still missed barge. FIXED playback rate: device rejects 22050 but PortAudio sometimes 'opened' it anyway -> ran at wrong rate; now probes a supported rate via check_output_settings up front and plays at 44100+resample (portable). FIXED floor-creep: _ambient_rms now FREEZES its rise on bursts (>+6dB) so a sustained talk-over can't raise its own bar (self-calibrating). HONEST CONCLUSION: open ALC285 barge-in is a HARDWARE LIMIT -- the post-AEC residual echo spikes (~0.1-0.3) overlap a normal voice (~0.2-0.4) and vary run-to-run, so a level/sustain gate has NO clean operating point (self_int measured non-monotonic across margins). Best-effort config left (margin 18, sustain 0.8). RELIABLE PATH = HEADPHONES (echo-free -> barge trivially works, snappy; min_speech~0.3, margin~6, aec off). Auto-calibration delivered: playback rate (per-device), AEC delay (FIFO -> ~0), noise floor (freeze-on-burst), coherence margin (EWMA) -- but can't beat the nonlinear-speaker echo. (6) SESSION-ANALYSIS FIXES (9dc9a55): voice fix + barge-fire confirmed live (barge_in_latency~0). Fixed 3 problems from run-20260602-235431: (a) AEC ran on EVERY block incl. no-playback -> DTLN distorted CLEAN speech -> ASR garble; now gated on far-end energy (RMS>1e-4). (b) SenseVoice 2nd-pass hallucinated tiny clips ('SO'->'I then.', 'WHAT'->'I.'); asr_final_min_sec 0.0->0.5 so sub-0.5s clips use the streaming final. (c) barge-in still used speaker-ID via speaker_gate_input; made it IDENTITY-FREE (removed identity from _looks_like_user; speaker-ID gates FINALS only now -- fully decoupled). Not code bugs: TTS queue depth 26 on a long answer = working-as-designed (LLM faster than TTS; barge/stop interrupts it; queue has drop-oldest backpressure); RAM/VRAM ~90% = the gemma3:12b+4b model sizes. 1282 passed. See docs/session_2026-06-02_playback_fifo_aec_alignment.md."
}