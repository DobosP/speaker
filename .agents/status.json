{
  "swarm": "speaker-improvement",
  "mode": "supervised-burst",
  "version": 1,
  "machine": {
    "cpu": "i9-13980HX (32 threads)",
    "ram_gib": 30,
    "gpu": "RTX 4090 Laptop (16 GB)",
    "python": "3.12",
    "target_profile": "desktop_gpu_4090"
  },
  "last_verdict": {
    "tests": "1958 passed, 25 skipped (.venv/bin/python -m pytest tests -q). FIVE waves + a LIVE test this session. Wave 5: LIVE-validated asr-tts-2 (async second pass ENGAGED, 0 underflows/exceptions/crashes/self-interrupts, barge-in cuts real talk-overs) BUT surfaced two problems: (1) the MIC IS CLIPPING 36-65% of blocks (peak=1.0, rms~0.5) -> OS mic gain too hot -> garbled STT + SenseVoice hallucinations ('I.'); OWNER must lower OS mic input level/disable mic-boost (input_gain=1.0 so it's not code). (2) owner CONFIRMED real acoustic OUTPUT artifacts (buzzy/glitchy TTS, not just garbled words) = the run-103630 white-noise still partly present; _audio_cb silently zero-fills FIFO underruns. ADDED DIAGNOSTICS (real-time-safe, reviewed SHIP): input-clipping warning in the capture heartbeat (clip=% + one-shot WARN >2%), output-underrun counter in _audio_cb (per-reply WARN + playback_underrun metric + heartbeat underruns=N). NEXT: owner lowers mic gain + re-runs -> clip% should ~0 (STT jumps) and underruns=N confirms whether the buzzy artifacts are FIFO underruns from CPU contention (async SenseVoice worker + TTS synth on the desktop CPU tier) -> if so, per-tier TTS/worker thread budgeting. Also: auto-detect picked 'desktop' not 'desktop_gpu_4090' (probe reads 16376MB < 16GB threshold -- recommend_profile off-by-rounding). --- Earlier waves 1-4: Wave 4 (low-value headless cleanup): llm-inference-9 (LlamaCppLLM type_k/type_v KV-cache quant plumbing; phone/phone_lite quantize the K cache to q8_0, V left at f16 since V-quant needs flash-attn; fail-soft on an old lib) + control-plane-2 (load-elastic admission: supervisor caps concurrency to 1 under sustained load via an ungated load_fraction reader threaded app->runtime->supervisor, separate from the live_routing-gated load_snapshot). DEFERRED with reasons: llm-inference-6 (per-call timeout blocked by the single-context lock; supervisor 25s reap backstops it), llm-inference-7 (Q3 weights config flip needs on-device quality validation). Reviewed (1 refuted, nits+1 LOW; LOW fixed -> K-only). --- Wave 3: Wave 3 (headless): control-plane-3 -- EWMA-scaled ADAPTIVE watchdog deadlines. The recorder now folds a TTS first-audio EWMA (recent_tts_ms) alongside the TTFT one; the watchdog's 'llm stuck'/'tts stuck' deadlines = clamp(4*recent_ewma, floor, ceil) with cold-start fallback to the static 10s/5s. LLM floor 6s (above a heavy-tier first-token, since the EWMA blends tiers), ceil 30s; ADAPTIVE_DEADLINES toggle pins legacy. Net: fast device surfaces a real stall sooner, slow phone stops false-flagging honest long turns (attacks the stuck:1 bundle noise). Diagnosis only -- supervisor task-reap is the real hang bound. Adversarially reviewed (0 refuted, nits+1 LOW, fixed). Scouted first: routing-cascade-6 already neutralized by LLMCapabilityRouter cache, routing-cascade-7 done, routing-cascade-4/asr-tts-3 need real models/audio. --- Wave 1+2: Wave 1: asr-tts-2 (async second-pass ASR) + asr-tts-1 (per-tier ASR policy). Wave 2 (headless, no live test): llm-inference-3 (LlamaCppLLM num_predict->max_tokens cap translation + phone/phone_lite caps) + rc-5 (SUPERSEDED metric so newest-input-wins-preempted turns don't false-flag 'llm stuck') + invariant hardening (no profile may disable speaker_gate_input / agent_brain.require_owner_verified). Both waves adversarially reviewed; wave-2 review CAUGHT A BLOCKER (a config _comment key leaking into llama.cpp's strict create_chat_completion signature) -> fixed by dropping _-prefixed option keys + a strict-signature regression test. Scouted Phase-1 first: device-adapt-1/cross-platform-8/cloud-invariant were already done.",
    "when": "2026-06-18",
    "machine": "i9-13980HX",
    "current": "1983 passed, 24 skipped (.venv/bin/python -m pytest tests -q). 2026-06-18: open-speaker SELF-INTERRUPT fixed at root + DTLN AEC enabled. Root cause (found by REPLAYING the owner's recording headlessly, not live): coherence decide() returned None during the ref-fill window -> _looks_like_user falls to the loud-mic level gate -> fires on the echo. Fix (5562d22): decide() returns False (echo) not None while the ref ring builds (ref.size>0 but <min). Stacked with the playback-onset grace (synth-anchored, 0.40s, f5b761a). NLMS proven useless headlessly (~0dB, echo is 88% nonlinear); DTLN proven the right backend (+6.2dB ERLE, 0 divergence, real-time-safe 22ms/block) -> provisioned (.venv now has onnxruntime+tf2onnx+tensorflow-cpu; setup_models --aec-model) + ENABLED (config.local.json aec_enabled=true aec_backend=dtln). New tooling makes barge AND aec-delay calibratable WITHOUT the mic: record_playback_reference (run-<id>.ref.wav, FarEndRing-aligned when AEC on) + tools/replay_barge.py + tools/aec_probe.py. Handoff: docs/session_2026-06-18_open_speaker_self_interrupt_and_dtln_aec.md."
  },
  "next": "2026-06-18 NEXT (open-speaker self-interrupt FIXED + DTLN AEC enabled; needs ONE live run): (1) Owner runs `./session.sh --llm echo` on the open speaker -> confirm self-interrupt is GONE on EVERY turn (was stops-responding), DTLN AEC doesn't misbehave, STT cleaner. It re-records a mic+FarEndRing-reference bundle. (2) THEN calibrate aec_ref_delay_ms HEADLESSLY from that new bundle: `python -m tools.aec_probe logs/runs/run-<new>.wav --backend dtln --device desktop --max-delay-ms 800` -> set aec_ref_delay_ms (currently placeholder 260) to the best-ERLE delay (now the LIVE value via the FarEndRing reference). (3) Residual STT garble on LOUD speech = the owner's mic clips at the ADC (hot OS gain) -- only lowering the OS mic / the clip-warning helps (no software un-clips). Tools to iterate WITHOUT the mic: tools/replay_barge.py (barge decision) + tools/aec_probe.py (ERLE/delay) over any run with record_playback_reference=true. config.local.json has: aec_enabled=true, aec_backend=dtln, asr_final_backend=sense_voice, asr_final_async=true, input_agc=FALSE (AGC breaks coherence barge -- do NOT re-enable), record_playback_reference=true. Handoff: docs/session_2026-06-18_open_speaker_self_interrupt_and_dtln_aec.md. --- PRIOR DONE 2026-06-17 pt3: asr-tts-2 + asr-tts-1 LANDED (headless, unit-tested + real-model smoke; suite 1920 green). asr-tts-2: the offline SenseVoice second pass now runs on a DEDICATED worker thread (_final_worker) off the capture loop, removing the measured cause of the run-103630 white-noise output + false-barge self-interrupts (the synchronous decode stalled the real-time thread). config SherpaConfig.asr_final_async (default True); engages only when a 2nd-pass recognizer is built (else inline, byte-identical). Extracted _finalize_and_dispatch (decode+floor+speaker gate+dispatch); _enqueue_final drops the OLDEST on overflow to keep capture-order dispatch (the runtime supersede is newest-ARRIVAL-wins). ONE final per utterance (NOT streaming-first-then-upgrade: verified the runtime can't upgrade in place, and the streaming final 'WHOLE HOLLO' is exactly what must NOT reach the LLM). asr-tts-1: phone_lite.sherpa now asr_final_backend='' + greedy_search (was inheriting heavy desktop beam+SenseVoice); desktop/4090 unchanged. core/engines/sherpa.py + config.json + tests/test_asr_final_async.py + tests/test_device_profiles.py. NOT YET LIVE-VALIDATED (needs the mic; --engine replay uses streaming-only FileReplayEngine). OWNER NEXT: re-enable SenseVoice in config.local.json (asr_final_backend='sense_voice', keep asr_final_async=true) and `./session.sh --llm echo` on the open speaker -> confirm vs run-103630 that white-noise+self-interrupts are GONE while the STT win remains; watch for the 'second-pass final ASR runs ASYNC' log line. THEN: audio-bargein-7 (DTLN deep-AEC), coherence short-talk-over tuning, speaker-ID re-enroll. Handoff: docs/session_2026-06-17_async_second_pass_asr.md. --- PRIOR pt2: ENV PROVISIONED + SPEAKER-ID ENROLLED + LIVE OPEN-SPEAKER BARGE-IN A/B (the P1 is now MEASURED, not just modelled). Env: installed the on-device runtime into .venv + setup_models (sherpa ASR/VAD/TTS + CAM++ speaker-ID + SenseVoice second pass, ~600MB in pretrained_models/sherpa) + Ollama has gemma3:12b/4b; doctor=READY. Owner enrolled speaker-ID. THE OWNER's interactive shell has .venv ACTIVATED so bare python3==.venv (anything touching models/audio MUST use .venv). LIVE A/B RESULTS (3 runs, logs/runs/run-20260617-1020/1028/1036; --llm echo, open laptop speaker, no headphones): (1) run-102023 AEC-off + SenseVoice-off = the BEST baseline: 5 talk-overs CUT, 3 short(0.2s) MISSED, **0 SELF-INTERRUPTS** (coherence-only path, echo baseline 0.70, misses sub-0.3s talk-overs). (2) run-102808 AEC-on NLMS@260ms = WORSE: NLMS filter DIVERGED ('AEC diverged ... reset+passthrough'), corrupted the mic -> worse STT + 1cut/5missed -> NLMS-AEC ruled out. (3) run-103630 AEC-off + SenseVoice-on = STT MUCH better (raw 'WHOLE HOLLO' -> final 'Helen, how are you.') BUT introduced SELF-INTERRUPTS + white-noise output artifacts -- the SenseVoice second pass runs SYNCHRONOUSLY on the capture thread (audit asr-tts-2) and contends with the real-time playback callback. PREPARED STATE for next run (config.local.json, machine-local/gitignored): aec_enabled=false, asr_final_backend='' (SenseVoice downloaded but OFF), speaker_gate_input=false -> the empirically-clean 0-self-interrupt baseline; `./session.sh --llm echo` runs it. NEXT DEV (code, headless-implementable then live-validate): (a) asr-tts-2 -- run the SenseVoice second pass ASYNC off the capture thread; NOW DATA-JUSTIFIED as the cause of the artifacts+self-interrupts, and the key to keeping the STT win without breaking real-time; (b) for open-speaker echo: the DTLN deep-AEC tier (audio-bargein-7), NOT the linear NLMS that diverged; (c) coherence-margin/confirm tuning for short talk-overs -- LIVE-only validation (the Phase-0 scorecard covers the AEC-on DTD path, not the coherence-only path). OWNER TO-DOS: re-enroll speaker-ID in the normal use position OR lower sherpa.speaker_threshold (~0.4) -- the live voice scored <0.50 vs enrollment so the input gate rejected the owner (that's why speaker_gate_input is off); then flip it back to true. Carried: cloud default-OFF; deferred pre-release hygiene gate (purge WAVs + transcript redaction + gitleaks CI, per decision D-B keep-dev-artifacts)."
}
