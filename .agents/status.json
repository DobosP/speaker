{
  "swarm": "speaker-improvement",
  "mode": "supervised-burst",
  "version": 1,
  "machine": {
    "cpu": "i9-13980HX (32 threads)",
    "ram_gib": 30,
    "gpu": "RTX 4090 Laptop (16 GB)",
    "python": "3.12",
    "target_profile": "desktop_gpu_4090"
  },
  "last_verdict": {
    "tests": "1528 passed, 13 skipped, 0 failed (.venv/bin/python -m pytest tests; psycopg present so the memory-contract + producer suites run)",
    "when": "2026-06-13",
    "machine": "i9-13980HX"
  },
  "next": "DONE 2026-06-13 (SMART/EFFICIENT MEMORY RECALL -- branch feat/smart-memory-recall-budget, commits 1f0a43c feat + c23f5cc review-fixes; landing to main). Goal: recall must not blow up the LLM context. Replaced 3 stacked blunt char caps (per-item [:150] RAM+PG, total [:600] at injection) + every fixed threshold (cosine>0.6, top-3, overlap>=2) with ONE token budget + a DATA-DERIVED adaptive cutoff. NEW always_on_agent/recall.py (stdlib-only, zero-utils-dep, SQLite/mobile-portable): Candidate, RecallBudget, estimate_tokens, adaptive_cutoff (scale-free DOMINANCE rule: largest gap >= 2x next-largest -- FP-robust, fires at n>=3), collapse (near-dup + summary-subsumes-span; profile exact-dedup only), pack (value-density knapsack measured on the RENDERED block), compress (whole-sentence, query-aware, label-aware, never mid-word), build_block, trim_block_to_tokens. Both SessionMemory (RAM) and MemoryManager (PG) are thin candidate-gatherers over build_block -> byte-identical block (parity gap closed). Profile is a SEPARATE budgeted sub-pass sharing ONE max_tokens with recall (reserved floor so episodic recall can't evict durable facts) -> combined <= budget. Default budget 150 tokens (== old 600-char//4, so enabling recall never raises volume). recall_max_chars kept as deprecated alias; dead recall_min_similarity/recall_max_items removed. Summarize trigger (_estimate_tokens) UNTOUCHED. Recall stays default-OFF (zero live change). Built via 2 ultracode workflows: 16-agent design (map->design->judge->synthesize->critique) + 28-agent adversarial review (20 findings, all real ones fixed). NEXT: (a) flip recall_enabled on after `python -m tools.bench` confirms a favorable token delta; (b) optional follow-up migrations/005 salience+pg_trgm hybrid (deferred, targets the proper-noun/STT-garble confabulation); (c) inject recall on the ReAct/escalated path too (today only assistant.answer). See docs/session_2026-06-13_smart_memory_recall.md."
}