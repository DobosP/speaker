{
  "swarm": "speaker-improvement",
  "mode": "supervised-burst",
  "version": 1,
  "machine": {
    "cpu": "i9-13980HX (32 threads)",
    "ram_gib": 30,
    "gpu": "RTX 4090 Laptop (16 GB)",
    "python": "3.12",
    "target_profile": "desktop_gpu_4090"
  },
  "last_verdict": {
    "tests": "1900 passed, 28 skipped (.venv/bin/python -m pytest tests -q). NOTE: scipy was pip-installed into the MINIMAL venv this session, which un-gated ~18 coherence tests (1882 -> 1900); without scipy the coherence path is inert and those self-skip. Use .venv/bin/python. Remaining ~28 skips are real_model/recorded/live_output-gated.",
    "when": "2026-06-17",
    "machine": "i9-13980HX"
  },
  "next": "DONE 2026-06-17 (ARCHITECTURE AUDIT -> ROADMAP -> PHASE 0 + PHASE 1 COMPLETE, all merged to main, 1900 green). 8-dimension fan-out architecture audit (docs/architecture_audit_2026-06-16.md: components are best-in-class; the gap is device adaptation -- the probe was advisory-only; 66 recs, 57 survived perf+privacy verification) -> phased plan (docs/roadmap_2026-06-17.md) under two OWNER DECISIONS: (D-A) open-speaker barge-in is a hard MUST; (D-B) keep logs/runs WAVs+summary+txt committed for dev -> the PII purge is DEFERRED to a pre-release gate, do NOT re-flag as P0 (see memory dev-artifacts-retention). LANDED ON main THIS SESSION: (1) Phase 0 barge-in SCORECARD + regression gate -- tests/barge_scorecard.py + tests/test_barge_scorecard.py + tests/fixtures/barge_in/phase0_scorecard.json; deterministic replay of the real shipped chain over the 203236/234435 traces: 0 self-interrupts, 2/2 talk-overs cut, max latency 0.407s, meets_requirement=true (commit 443df8a). (2) Phase 1 device spine: device-adapt-1 (config.device='auto' -> tools.recommend_profile auto-applies the probed profile at launch; a GPU box still resolves to 'desktop', zero change) + cross-platform-8 (fail-fast on unknown --device) + cloud-safety invariant test tests/test_device_profile_invariants.py (1412c83). (3) llm-inference-2: _auto_llm_threads budgets the llamacpp thread count against the audio path so a reply can't starve barge-in (ae27e6e). (4) llm-inference-5: setup_models --gguf + requirements-ondevice.txt + a guided SystemExit make the on-device llama.cpp tier provisionable (f2405c2). (5) audio-bargein-8: moved coherence note_playback off the real-time PortAudio callback to a lock-free deque drained on the capture thread; behaviour-preserving (equivalence test), unblocks raising coherence_ring_ms (bd2e224). (6) BONUS: runlog prune_old_runs no longer auto-deletes git-tracked run bundles -- it was silently deleting the committed barge WAVs on every `python -m core` startup (12bfe17). NEXT (pick up here): PHASE 2 barge-in robustness -- audio-bargein-2 (wire the EXISTING AEC-delay auto-calibration EchoCoherenceDetector.measured_delay_samples), audio-bargein-1 (per-profile audio-tuning block; now safe to raise coherence_ring_ms after audio-bargein-8), asr-tts-4 (streaming TTS w/ echo floor), audio-bargein-5 (promote Smart Turn v3 prosody), audio-bargein-6 (pVAD veto). EACH gated on the Phase-0 scorecard + a LIVE open-speaker A/B. OWNER TO-DOS (only the owner; all still open): enroll speaker-ID (python -m core --enroll; gates the pVAD veto + actuator/cloud delegation); run a LIVE open-speaker barge-in A/B on the bare speaker (./session.sh --record) to convert the field-unvalidated P1 to measured; run python -m tools.bench --profile phone on real/throttled HW to replace specsim estimates. Carried: cloud default-OFF; the deferred pre-release hygiene gate (purge WAVs + transcript redaction + gitleaks CI) per D-B."
}
