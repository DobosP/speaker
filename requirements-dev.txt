# Dev/test extra on top of the lean runtime deps -- install with `pip install -r requirements-dev.txt`.
#
# These are NOT needed to run the assistant; they back the test suite + tools/run_tests.py.
# (psycopg is already in requirements.txt for the memory subsystem, but is pinned here too so
# the @pytest.mark.postgres integration tests have their driver even on a runtime-only box.)

pytest>=9.1.1                     # the test runner (`pytest tests` / tools/run_tests.py)
pytest-xdist>=3.8.0             # parallel run: `pytest -n auto` (~9s vs ~35s serial)
pytest-timeout>=2.4.0           # per-test hang guard (pytest.ini: --timeout=60 --timeout-method=thread)
psycopg[binary,pool]>=3.3.4     # PostgreSQL driver + pool; gates the @pytest.mark.postgres memory tests
