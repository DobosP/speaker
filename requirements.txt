# Cross-Platform Voice Assistant Dependencies
# Install with: pip install -r requirements.txt

# Audio (cross-platform)
sounddevice>=0.4.6
pygame>=2.5.0

# Text-to-Speech (multiple backends)
edge-tts>=6.1.0          # Fast, natural Microsoft voices (recommended)
gTTS>=2.4.0              # Google TTS fallback

# Speech-to-Text
faster-whisper>=1.0.0    # 4x faster than standard Whisper (recommended)
transformers>=4.36.0     # Standard Whisper fallback
torch>=2.0.0
accelerate>=0.26.0

# Audio processing
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0

# LLM
ollama>=0.1.0

# Memory & Database (optional but recommended)
psycopg2-binary>=2.9.0   # PostgreSQL driver
sentence-transformers>=2.2.0  # For embeddings/vector search

# Environment
python-dotenv>=1.0.0
